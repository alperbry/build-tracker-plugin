package com.alperbry.buildtracker.data.extension

// todo to be extended
open class BuildTrackerExtension(
    var id: String? = null
) {
    companion object {
        const val EXTENSION_NAME = "buildTracker"
    }
}
